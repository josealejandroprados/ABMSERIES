<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Administrador de Series</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
</head>
<body>
    
    <div class="container-fluid mt-4">

        <h4 class="h4 py-2">Administrador de Series</h4>
        <div class="table-responsive">

            <table class="table table-bordered table-hover" id="tabla">
                <thead>
                    <tr>
                        <th>n</th>
                        <th>Titulo</th>
                        <th>Descripcion</th>
                        <th>Fecha de Estreno</th>
                        <th>Estrellas</th>
                        <th>Genero</th>
                        <th>Precio de Alquiler</th>
                        <th>ATP</th>
                        <th>Estado</th>
                    </tr>
                </thead>
                <tbody id="tbody">
                    <% if(data.length>0) { %>
                        <% for(var i=0; i<data.length; i++){ %>
                            <% var id = data[i].id_serie %>
                            <tr onclick="getI(<%=id%>,<%=i%>)">
                                <td><%= (i+1) %></td>
                                <td><%= data[i].titulo %></td>
                                <td><%= data[i].descripcion %></td>
                                <td><%= data[i].fecha %></td>
                                <td><%= data[i].estrellas %></td>
                                <td><%= data[i].genero %></td>
                                <td><%= data[i].precio %></td>
                                <td><%= data[i].atp %></td>
                                <td><%= data[i].estado %></td>
                            </tr>
                        <% } %>
                    <%}%>
                        
                </tbody>
            </table>
            <% if(data.length==0) { %>
                <span id="sinRegistros">¡No hay registros!</span>
            <%}%>
        </div>
    </div>

    <footer>
        <div class="container mb-5">
            <div class="row">
                <div class="col-10">
                    <button type="button" class="btn btn-primary" id="btnNuevo">Nuevo</button>
                    <button type="button" class="btn btn-success" id="btnModificar">Modificar</button>
                    <button type="button" class="btn btn-warning" id="btnAnular">Anular</button>
                    <button type="button" class="btn btn-danger" id="btnEliminar">Eliminar</button>
                </div>
                
                <div class="col-2">
                    <a href="/salir" class="btn btn-secondary" id="btnSalir">Salir</a>
                </div>
            </div>
        </div>
    </footer>


    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
    <script src="home.js"></script>
    
</body>
</html>